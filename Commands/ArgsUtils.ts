/** Enumerator for more explicit indexes */
export const ArgumentIndexes = Object.freeze({ "MESSAGE":0 });

/**
 *  ArgumentMode.SPLIT Text after the command will be split with the space character and each will be a separate argument
 *  ArgumentMode.WHOLE Everything written after the command will be but in one argument
 */

export const ArgumentMode = Object.freeze({ "SPLIT":0 , "WHOLE":1 });